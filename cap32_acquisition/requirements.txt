# Core — acquisition GUI, filters, impedance UI, and all hardware/acceptance tests
numpy<2.0
scipy
pyqtgraph
PyQt6
matplotlib          # test plots (hardware_check / alpha_check / test_injection / formula)
mne                 # scalp topomaps (cap_gui head-map, impedance map, alpha topomap)

# Optional (everything above works without these):
#   autoreject, mne-icalabel  — only for full ICA de-blink calibration / offline cleaning
#   pylsl                      — only for the LSL-bridge path (udp_lsl_bridge.py as LSL source)
# pip install autoreject mne-icalabel pylsl
